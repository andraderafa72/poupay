import styled from "styled-components";

export const Container = styled.section`
  max-width: 1320px;
  margin: 0 auto 7rem;
  padding: 2rem;

  p{
    color: var(--text-body);
    font-size: 1rem;
    text-align: justify;
  }

  @media(max-width: 1024px){

  }
`;